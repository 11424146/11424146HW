# Engineering Mathematics Homework 2

Class: CS A  
Student ID: `11424146`  
Name: `鄭沂加`  
Date: 2026/06/04

---

## Laplace Transform (Topic 2)

### 1. Problem

Solve the ODE with Laplace transform:

$$
\frac{d^2x}{dt^2} - 3\frac{dx}{dt} + 2x = \delta(t)
$$

Initial conditions:

$$
x(0)=0,\quad x'(0)=0
$$

Here `delta(t)` is the unit impulse at `t=0`.

Goal: find `x(t)` for `t>=0`.

---

### 2. Transform to the s-Domain

Let `X(s)` be the Laplace transform of `x(t)`.

$$
L\{x'(t)\}=sX(s)-x(0)=sX(s)
$$

$$
L\{x''(t)\}=s^2X(s)-sx(0)-x'(0)=s^2X(s)
$$

$$
L\{\delta(t)\}=1
$$

Put these into the ODE:

$$
s^2X(s)-3sX(s)+2X(s)=1
$$

$$
X(s)(s^2-3s+2)=1
$$

Factor the denominator:

$$
s^2-3s+2=(s-1)(s-2)
$$

So

$$
X(s)=\frac{1}{(s-1)(s-2)}
$$

---

### 3. Partial Fractions

Write

$$
\frac{1}{(s-1)(s-2)}=\frac{A}{s-1}+\frac{B}{s-2}
$$

Multiply by `(s-1)(s-2)`:

$$
1=A(s-2)+B(s-1)
$$

Let `s=1`:

$$
1=A(-1) \Rightarrow A=-1
$$

Let `s=2`:

$$
1=B(1) \Rightarrow B=1
$$

So

$$
X(s)=-\frac{1}{s-1}+\frac{1}{s-2}
$$

---

### 4. Solution

Use

$$
\mathcal{L}^{-1}\Bigl\{\frac{1}{s-a}\Bigr\}=e^{at}
$$

For `t>0`:

$$
x(t)=-e^{t}+e^{2t}=e^{2t}-e^{t}
$$

---

### 5. Meaning of the Result

1. At `t=0`, we get `x(0)=1-1=0`. This matches the initial condition.  

2. The derivative is

$$
x'(t)=2e^{2t}-e^{t}
$$

At `t=0+`, `x'(0+)=1`.  
For an impulse input, the slope can jump at `t=0`. The given `x'(0)=0` is the value before the impulse.

3. As `t` grows, the term `e^{2t}` grows faster than `e^t`, so the answer is dominated by `e^{2t}` for large `t`.

---

### 6. Example

If we only check the numbers at `t=1`:

$$
x(1)=e^{2}-e \approx 4.67
$$

---

### 7. Conclusion

The Laplace method turns the ODE into algebra in `s`, then we go back to time with inverse transform.  
Final answer:

$$
x(t)=e^{2t}-e^{t},\quad t\ge 0
$$

---

## Fourier Series (Topic 7)

### 1. Problem

Find the Fourier series of

$$
f(x)=x+\pi
$$

on

$$
-\pi \le x \le \pi
$$

Goal: find `a_0`, `a_n`, `b_n`, and write the full series.

---

### 2. Standard Form

On `[-pi, pi]`:

$$
f(x)=\frac{a_0}{2}+\sum_{n=1}^{\infty}(a_n\cos nx+b_n\sin nx)
$$

$$
a_0=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\,dx
$$

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\cos(nx)\,dx
$$

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\sin(nx)\,dx
$$

---

### 3. Find `a_0`

$$
a_0=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\,dx
$$

Split the integral:

$$
a_0=\frac{1}{\pi}\left(\int_{-\pi}^{\pi}x\,dx+\int_{-\pi}^{\pi}\pi\,dx\right)
$$

The first part is `0` (odd function).  
The second part is `2*pi^2`.

So `a_0=2*pi` and

$$
\frac{a_0}{2}=\pi
$$

---

### 4. Find `a_n`

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\cos(nx)\,dx
$$

- The integral of `x*cos(nx)` from `-pi` to `pi` is `0` (odd times even).  
- The integral of `cos(nx)` from `-pi` to `pi` is `0`.

So `a_n=0` for all `n>=1`.

---

### 5. Find `b_n`

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\sin(nx)\,dx
$$

The constant `pi` part gives `0`. So

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}x\sin(nx)\,dx
=\frac{2}{\pi}\int_{0}^{\pi}x\sin(nx)\,dx
$$

(last step: `x sin(nx)` is even).

Integration by parts with `u=x`, `dv=sin(nx) dx`:

$$
\int_{0}^{\pi}x\sin(nx)\,dx
=-\frac{x\cos(nx)}{n}\Big|_{0}^{\pi}
+\frac{1}{n}\int_{0}^{\pi}\cos(nx)\,dx
$$

The second integral is `0`. So

$$
\int_{0}^{\pi}x\sin(nx)\,dx=-\frac{\pi\cos(n\pi)}{n}
=-\frac{\pi(-1)^n}{n}
$$

Then

$$
b_n=\frac{2(-1)^{n+1}}{n}
$$

---

### 6. Write the Series

$$
f(x)=\pi+\sum_{n=1}^{\infty}\frac{2(-1)^{n+1}}{n}\sin(nx)
$$

Same form:

$$
f(x)=\pi+2\sum_{n=1}^{\infty}\frac{(-1)^{n+1}}{n}\sin(nx)
$$

---

### 7. Conclusion

Only the constant `pi` and sine terms stay in the series.  
At `x=0`, both sides give `pi`.  
This matches the idea of Topic 7: a simple function on `[-pi,pi]` can be written as a Fourier series.

Final answer:

$$
x+\pi=\pi+2\sum_{n=1}^{\infty}\frac{(-1)^{n+1}}{n}\sin(nx)
$$

---

## References

1. Course handout: first-order and second-order ODE, Laplace transform, Fourier series.  
2. Format reference: [題目二_降落傘終端速度_期中報告.md](./題目二_降落傘終端速度_期中報告.md)
