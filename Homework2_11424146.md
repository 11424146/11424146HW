# Engineering Mathematics — Homework 2

Class: CS A  
Student ID: `11424146`  
Name: `鄭沂加`  
Date: 2026/06/04

Topics (from class list):

- Laplace transform: **(2)**
- Fourier series: **(7)**

---

## Part A — Laplace Transform (Topic 2)

### 1. Problem

Solve this ODE with Laplace transform:

$$
\frac{d^2x}{dt^2} - 3\frac{dx}{dt} + 2x = \delta(t)
$$

Initial values:

$$
x(0)=0,\quad x'(0)=0
$$

Note: $\delta(t)$ is the unit impulse at $t=0$.

---

### 2. Laplace of Each Term

Let $X(s)=\mathcal{L}\{x(t)\}$.

We use:

$$
\mathcal{L}\{x'(t)\}=sX(s)-x(0)=sX(s)
$$

$$
\mathcal{L}\{x''(t)\}=s^2X(s)-sx(0)-x'(0)=s^2X(s)
$$

$$
\mathcal{L}\{\delta(t)\}=1
$$

Put them into the ODE:

$$
s^2X(s)-3sX(s)+2X(s)=1
$$

$$
X(s)(s^2-3s+2)=1
$$

Factor:

$$
s^2-3s+2=(s-1)(s-2)
$$

So

$$
X(s)=\frac{1}{(s-1)(s-2)}
$$

---

### 3. Partial Fractions

Write

$$
\frac{1}{(s-1)(s-2)}=\frac{A}{s-1}+\frac{B}{s-2}
$$

Multiply both sides by $(s-1)(s-2)$:

$$
1=A(s-2)+B(s-1)
$$

Let $s=1$: $1=A(-1)\Rightarrow A=-1$.

Let $s=2$: $1=B(1)\Rightarrow B=1$.

So

$$
X(s)=-\frac{1}{s-1}+\frac{1}{s-2}
$$

---

### 4. Inverse Laplace

We know

$$
\mathcal{L}^{-1}\left\{\frac{1}{s-a}\right\}=e^{at}
$$

So for $t>0$:

$$
x(t)=-e^{t}+e^{2t}=e^{2t}-e^{t}
$$

---

### 5. Check

At $t=0$:

$$
x(0)=1-1=0
$$

This matches $x(0)=0$.

The derivative is

$$
x'(t)=2e^{2t}-e^{t}
$$

At $t=0^+$ we get $x'(0^+)=1$.  
That is normal for an impulse input: the slope can jump at $t=0$, while the given $x'(0)=0$ is the value **before** the impulse.

---

### 6. Answer (Part A)

$$
\boxed{x(t)=e^{2t}-e^{t},\quad t\ge 0}
$$

---

## Part B — Fourier Series (Topic 7)

### 1. Problem

Find the Fourier series of

$$
f(x)=x+\pi,\qquad -\pi\le x\le \pi
$$

---

### 2. Standard Form

On $[-\pi,\pi]$:

$$
f(x)=\frac{a_0}{2}+\sum_{n=1}^{\infty}\bigl(a_n\cos nx+b_n\sin nx\bigr)
$$

$$
a_0=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\,dx
$$

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\cos(nx)\,dx,\qquad
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\sin(nx)\,dx
$$

---

### 3. Find $a_0$

$$
a_0=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\,dx
=\frac{1}{\pi}\left(\int_{-\pi}^{\pi}x\,dx+\int_{-\pi}^{\pi}\pi\,dx\right)
$$

The first integral is $0$ (odd function on symmetric interval).

The second one is $2\pi^2$.

So $a_0=2\pi$ and

$$
\frac{a_0}{2}=\pi
$$

---

### 4. Find $a_n$

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\cos(nx)\,dx
$$

- $\int_{-\pi}^{\pi}x\cos(nx)\,dx=0$ because $x\cos(nx)$ is odd.
- $\int_{-\pi}^{\pi}\cos(nx)\,dx=0$.

So $a_n=0$ for all $n\ge 1$.

---

### 5. Find $b_n$

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}(x+\pi)\sin(nx)\,dx
$$

The constant $\pi$ gives zero:

$$
\int_{-\pi}^{\pi}\sin(nx)\,dx=0
$$

So only the $x$ part matters:

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}x\sin(nx)\,dx
=\frac{2}{\pi}\int_{0}^{\pi}x\sin(nx)\,dx
$$

(last step: $x\sin(nx)$ is even).

Integration by parts: $u=x$, $dv=\sin(nx)\,dx$.

$$
\int_{0}^{\pi}x\sin(nx)\,dx
=-\frac{x\cos(nx)}{n}\Big|_{0}^{\pi}
+\frac{1}{n}\int_{0}^{\pi}\cos(nx)\,dx
$$

The second integral is $0$ because $\sin(n\pi)=0$.

So

$$
\int_{0}^{\pi}x\sin(nx)\,dx=-\frac{\pi\cos(n\pi)}{n}
=-\frac{\pi(-1)^n}{n}
$$

Then

$$
b_n=\frac{2}{\pi}\cdot\left(-\frac{\pi(-1)^n}{n}\right)
=\frac{2(-1)^{n+1}}{n}
$$

---

### 6. Write the Series

Only sine terms appear (plus the constant $\pi$):

$$
f(x)=\pi+\sum_{n=1}^{\infty}\frac{2(-1)^{n+1}}{n}\sin(nx)
$$

Same thing:

$$
f(x)=\pi+2\sum_{n=1}^{\infty}\frac{(-1)^{n+1}}{n}\sin(nx)
$$

---

### 7. Quick Check

At $x=0$: left side $f(0)=\pi$, right side $=\pi$ (all sine terms are $0$). OK.

At $x=-\pi$: $f(-\pi)=0$, and $\sin(-n\pi)=0$, so the series gives $\pi$.  
On $[-\pi,\pi]$, a Fourier series may not match the function at endpoints; that is expected for this type of problem.

---

### 8. Answer (Part B)

$$
\boxed{
x+\pi=\pi+2\sum_{n=1}^{\infty}\frac{(-1)^{n+1}}{n}\sin(nx)
}
$$

---

## References

1. Course handout: *第二次作業題目(數學觀點).docx*  
2. My HW1 style: [題目二_降落傘終端速度_期中報告.md](./題目二_降落傘終端速度_期中報告.md)
